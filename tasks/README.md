# Bitcoinhull — the 10 tasks

GOAL: a live, sleek Bitcoin dashboard ("the Hull") in the spirit of Clark
Moody's dashboard — more readable, cleaner, fewer useless stats, a few new
ones. After task 10 closes, v1 is LIVE on GitHub Pages.

**v1 PROJECT DONE + LIVE (2026-07-17)** — every v1 task ☑, `v1.0.0` tagged,
**https://bitcoinhull.com live end-to-end**: DNS flipped, cert issued,
HTTPS enforced, in-browser WS pass green. Done-definition fully met
(closer log in task 10's QA.md). **v1.1 FEATURE-COMPLETE 2026-07-17:
12 ☑ 13 ☑ 14 ☑ 15 ☑ (Clark expansion + Hull Integrity recomposition,
same-day Joe spec) — `v1.1.0` tagged per Joe's "finish everything,
don't ask permissions".**

## How to use (token-efficient)

Each task is self-contained and ≈10% of the project. To execute one:
`/clear`, then read ONLY `tasks/_SHARED.md` + the one `tasks/task-NN-…/TASK.md`.
Do the work, verify in the browser, commit (one commit per task), tick it off
here, tell Joe it's safe to `/clear`, stop.

Standing rules live in `_SHARED.md` (stack, data source, design tokens, panel
conventions, verification bar). Rules of the house live in `CLAUDE.md`.

## The tasks (execution order = priority order)

| # | Task | Depends on | Status |
|---|------|-----------|--------|
| 01 | Scaffold + design system + full static layout (every panel, placeholder data) | — | ☑ DONE (2026-07-16; 7 panels + strip, tokens locked, consistent placeholders from height 954,321; verified 1280/1000/375, console clean, no page h-scroll) |
| 02 | Data core: REST poller, store (pub/sub), formatters, live status chip | 01 | ☑ DONE (2026-07-16; 8 endpoints polling, chip POLLING→LIVE/DEGRADED/DOWN all exercised live incl. down→recover, backoff 5s→5min cap + 15s abort + stale-attempt seq guard, 20/20 formatter checks, console clean in every state, silent without ?debug=1) |
| 03 | Chain-tip panel live: height, time-since-block, recent-blocks strip | 02 | ☑ DONE (2026-07-16; header height + 6-block strip + NEXT tile live (matched mempool.space, caught 2 real blocks mid-test), ages/since tick on 30 s, SLOW warn/serious + STALE tag + recovery all exercised live, flash on new height, console clean in every state) |
| 04 | Fees panel live: recommended tiers + plain-English fee verdict | 02 | ☑ DONE (2026-07-16; tiers + hero live, matched mempool.space; verdict bands all 5 forced at both edges incl. missing-input → dash; clearing rate derived from the fee histogram, cross-checked against mempool.space's own next-block floor; stale drill live — last values kept + STALE 2 MIN + verdict dashes, then recovered to LIVE; console clean in every state) |
| 05 | Mempool panel live: depth, blocks-to-clear, projected-blocks mini-viz | 02 | ☑ DONE (2026-07-16; hero/pending/blocks-to-clear live + matched API; 8-seg ordinal ramp validated (--ordinal, both surfaces), overflow tail capped 2× + "+N blocks" honesty label with compact fallback, gap-aware measured label fit, role=img + live aria summary, caption w/ units; Incoming row DROPPED (research: two-poll delta is sign-flipped net flow — see task 09 for the honest source); stale drill + quiet/boundary/malformed/empty forced; console clean; no h-scroll at 375) |
| 06 | Price panel live: USD, sats-per-dollar, market cap | 02 | ☑ DONE (2026-07-16; hero matched mempool.space exactly, sats/$ hand-checked, mcap within 0.13% of CoinGecko's public figure; `HULL.supplyAt` shipped in js/supply.js — exact integer-sat era sum, boundary values at 0/209,999/840,000/asymptote all textbook-exact, NaN-safe (task 08 reuses it, noted in _SHARED.md); dim→normal `.flash-dim` on change, no re-flash on unchanged tick; stale drill live — values kept + STALE 3 MIN + dimmed, then recovered to LIVE; console clean in every state; no h-scroll at 375) |
| 07 | Mining panel live: hashrate, difficulty, retarget ETA + estimate | 02 | ☑ DONE (2026-07-16; hero 888 EH/s + difficulty 127.2 T matched mempool.space's mining page exactly, adjustment estimate within a poll of theirs (−0.7→−0.9%), retarget 1,280 blocks · ≈ 9 d matched API; hashrate ≈ diff×2³²/600 cross-check within 2.4%; epoch bar width == printed % by construction (both from one fmt.pct string, clamped, dash ⇒ 0-width); 10-min stale drill live — at 4 min the 300 s feeds correctly held un-dimmed while fast panels staled, at 11 min STALE 11 MIN + dim with last values kept, then recovered to LIVE with a fresh estimate; console clean in every state; no h-scroll at 375) |
| 08 | Supply + halving panels live: exact client-side issuance math, countdown | 02, 03 | ☑ DONE (2026-07-16; issued 20,057,309 BTC / 95.51% bar / 3.125 subsidy / 0.82% inflation + halving 91,662 blocks · ≈ Apr 2028 · 1.5625 next · Era 5 at 56.4% all live at tip 958,338, tracked a real block mid-test; supplyAt boundary checks textbook-exact — NOTE: TASK.md's literal sanity heights assumed first-H-blocks semantics, the shipped helper counts blocks 0..H incl. the tip (equivalent checks at 209,999/839,999 pass exactly; deviation reported to Joe); supply matches blockchain.info's public figure to 2 blocks of counter lag; fmt.pct grew an optional dp arg (backward-compatible) + new fmt.monthYear, both noted in _SHARED.md; stale drill live — both panels dim together + STALE 2 MIN + last values kept, then recovered to LIVE; console clean in every state) |
| 09 | Live layer: WebSocket upgrade, new-block flash, live title, poll fallback | 03–08 | ☑ DONE (2026-07-16; js/live.js pushes → same store keys, ZERO panel changes; 5 real blocks landed live (958,341–345), tile+header flash + instant title, beat mempool.space's own page by 5.0 s (bar: ~1 s); chip LIVE=WS/POLLING=REST-only, REST ×5 on the 4 WS-heartbeated keys ONLY (tipHeight/blocks/prices full cadence — stretched block polls would flap panel staleness; deviation documented); drills: stop()→POLLING, garbage-URL→5s→10s backoff, REST-garbage+WS→DEGRADED, all recovered; histogram carry expires 5 min→honest dash (drilled live 5m20s), title appends ·STALE >180s (drilled + cleared); REST regression guard (accept filters, drilled with planted tip); flap protection 30 s; fmt.usdK 16/16; reduced-motion cascade-proven on all 3 flash classes; Fable review fan-out 4 finders+8 verifiers+critic — 7 real findings all fixed & re-verified; console clean every state; heap flat) |
| 11 | BitcoinHull Integrity: 0–100 network-health score live (bar + breakdown dropdown; markup shipped 2026-07-16) — ⚠ COMPOSITION SUPERSEDED by task 15's recomposition (Joe, 2026-07-17): now 5 macro stats vs 3-yr averages; the row below documents the original build | 02 | ☑ DONE (2026-07-17; weights 25/25/20/20/10 as spec'd (Joe confirmed at start) — cadence = worse of since-block/15-block-avg on the 10→60 min ramp, mining vs diff×2³²/600 baseline, fees log-band 5→300, mempool 3→50 blocks-to-clear, freshness meters all 8 pipeline keys + conn=down→0; score = sum of rounded rows so the breakdown always adds up; all 4 verdict bands + missing-input dash forced live; kill-network drills (socket dead + REST garbage): STALE tag + dim, score sags ONLY via freshness — cadence freezes at its last fresh-feed value (review catch: the since-clock must not score a measurement outage), full recovery both times; review fan-out 3 finders + 5 adversarial verifiers + critic → every confirmed finding fixed & re-drilled; caret aria-hidden; console clean in every state; no h-scroll at 375) |
| 10 | Polish + ship: final QA pass, GitHub repo + Pages deploy + bitcoinhull.com, README, evidence | ALL | ☑ DONE (2026-07-17; QA sweep all green — 1440/1000/768/375 no h-scroll, page-wide down drill 7/8 panels tagged + mining honest per its 300 s threshold (task-07 11-min evidence), recovery without reload, cold load 49 ms, no NaN/undefined, 54/54 tabular-nums, Chrome+Edge identical (⚠ Firefox not installed = named gap), 24 h soak inferred from task-09 heap-flat (named); footer → attribution/GitHub/v1.0.0 + permanent Clark credit; repo github.com/abysal32-arch/bitcoinhull public, Pages built from `bac7805`, CNAME committed, custom domain wired (github.io 301s → bitcoinhull.com; GitHub edge verified serving the Hull for that host pre-DNS via --resolve), `v1.0.0` tagged; QA.md committed. CLOSERS DONE 2026-07-17: DNS live (GoDaddy WebsiteBuilder A record was the parking culprit), cert approved apex+www exp 2026-10-15, Enforce-HTTPS on (http 301s), in-browser https pass LIVE chip over wss + 0 console messages — done-definition fully met, log in QA.md) |
| 12 | v1.1 — Nodes panel: Luke Dashjr daily node file, est. total + listening + 30d trend | 10 | ☑ DONE (2026-07-17; ⚠ TWO spec corrections: (1) TASK.md's column read was WRONG — Luke's own chart JS sums col2+col3 for the total, col3 alone = non-listening estimate; implemented his way, hero ≈ 99.9k. (2) his server sends the CORS header TWICE → EVERY browser rejects it (curl "verified" ACAO by seeing the header — the dup makes it invalid; proven in Chromium + both HTTP versions). 12-agent research to exhaustion: bitnodes.io DEAD since 05-03, no alternative has current+history → shipped BAKED fallback (js/data/nodes.js, scripts/bake-nodes.sh, monthly) + live poll still armed = self-heals when Luke fixes his header. api.js grew opts {parse, aux, backoffCapMs} + setPath drill lever. Values proven via real-data fixture through the real pipeline: 99,854 / 5,120 / +8.5% (matched independent computation exactly); stale + garbage-URL + recovery drills green; conn chip immune (aux); console clean) |
| 13 | v1.1 — Treasuries: baked bitcointreasuries.net snapshot + as-of tag, monthly re-bake | 10 | ☑ DONE (2026-07-17; shipped as a MINOR card per Joe's same-day tier spec (supersedes the major-panel rows: corp/ETF split + top holder dropped, his 3 stats kept); baked totalBtc 2,286,345 read fresh from bitcointreasuries.net (18× in page blob; Clark showed 2,285,604 same day = hours of lag); USD value + supply-% computed LIVE from price feed + supplyAt so they track between re-bakes ($145.5B / 11.4% — Clark cross-check ✓); as-of visible = the honesty (no stale tag, task-13 rule now in _SHARED.md); credit permanent) |
| 14 | v1.1 — Incoming-flow row: socket-only vB/s on the mempool panel (the honest stat task 05 dropped, source found in 09) | 10 | ☑ DONE (2026-07-17; `vBytesPerSecond` pushes → store `vbps` (10 s throttle, joins the socket-health consumed set), `fmt.vbps` unit-switching formatter, Incoming row live at 5.6–6.4 kvB/s (sane: inflow vs ~2.7 kvB/s clearing matches the observed 44→46 vMB backlog creep); honesty rules exactly as spec'd — value ONLY while conn=`live`, `stop()` drill: row dashed INSTANTLY while hero/pending kept, NO stale tag, NO dim (vbps excluded from FEEDS with the deviation commented), `start()` → value back in seconds; cold boot dashes; console clean; 375 no-h-scroll) |
| 16 | v1.2 opener — REAL 24 h production soak: observe the three >12 h-uptime fixes live (two-phase: `task 16 begin` evening / `task 16 verify` next day; tab must stay open) | 15, 17 | ☑ DONE (Phase B PASS 7/7 at t0+28.5 h, 2026-07-19 22:45Z: zero console msgs the whole soak, zero false staleness [baked-nodes exemption held >2 d, treasuries USD tracked, 6 h feeds advanced], heap +164 KB flat, tip == mempool.space exact, reload → clean fresh boot on `6f0f5e9` [89 SOLID, POLLING→LIVE in flap window, dropdown builds 5 live now-vs-avg rows]; verdict in WORKLOG; screenshots unavailable — DOM/JS evidence) |
| 17 | v1.2 — real-time data + density redesign (Joe, 2026-07-18): major re-order around Price, ~50% shrink (minors half that), NEW Transactions major (blockchain.info, Clark-parity 7.8 tx/s / 4,695 per block), Blockchain height-first, mining panel renamed Chain security (standalone security panel + minersRevenue deleted), treasuries LIVE via CoinGecko (bake was WRONG: 2.29M matched no category, live 1.285M corroborated 4-way incl. bitcointreasuries.net itself), OP_RETURN row dropped (no live source EXISTS — opreturn.org died ~May 2026; Joe vetoed bake labels), integrity strip restyle same day (`349dfd4`) | 15 | ☑ DONE (2026-07-18; all values live incl. treasuries 1,285,045/179 cos and tx 7.8 tx/s / 4,695 per block matching Joe's Clark sample; kill-network drill: conn DOWN, 13/14 panels tagged+dim with values kept, Incoming dashed instantly, nodes correctly exempt (baked as-of), title STALE prefix at threshold, full recovery to LIVE + 0 stale; 375px no-h-scroll; console clean every state) |
| 18 | v1.2 — live scalars (Joe, 2026-07-18): Blockchair live all-time tx + chain size (fresh-preferred, chart fallback, fragile-origin contract) + HOURLY UTXO via utxo-count 1-week timespan; research verdicts: node count = NO live source EXISTS (bitnodes domain expired; hacknodes.xyz revival + KIT DSN both CORS-less; bitnod.es no API), Lightning = mempool's month-old snapshot is the best CORS-open option (stall is their side; bitcoinvisuals 1-2d fresh but zero CORS); CORS-request email drafts for both in task folder — Joe sends | 17 | ☑ DONE (2026-07-18; hero 1,399,576,501 live vs chart 1,398,317,951 (+1.26M lag erased), UTXO hourly 167,300,401, size 755.2 GB live; drills: bad-value → chart fallback with NO false stale tag, dead-origin poll → silent + store intact, recovery clean; 375px; console clean) |
| 19 | v1.2 — one equal tier (Joe, 2026-07-18): Blockchain↔Supply swap, Fees under Mempool, minor tier retired (treasuries+Lightning at full scale fill row 4), nodes baked-date line removed (stale exemption kept), density notch → ALL 12 panels + Integrity fit one 1080p viewport | 17 | ☑ DONE (2026-07-18; 12th-panel bottom 902px at 1280×940, order verified, console clean, 375px no-h-scroll) |
| 20 | v1.2 — brand ship (Joe, 2026-07-18): warship logo right of the wordmark (₿-21 hull number, ₿ pennant, genesis-hash SHA-256 armor belt), favicon → hull+₿; applied with the un-parked task-19 layout | 19 | ☑ DONE (2026-07-18; header 50px, 12-panel fit held at 902px/940, 375px brand fits no h-scroll, console clean) |
| 21 | v1.2 — integrity detail + full-page fit + bigger ship (Joe, 2026-07-18): dropdown shows now-vs-3yr values (weights hidden), UTXO metric redefined per-epoch LOWER-is-better (score 80→100 SOLID, deliberate), chain tip now on-screen at landing (938px @1280×940), ship 88×47, title grammar confirmed as-is | 19, 20 | ☑ DONE (2026-07-18; rows live: nodes 99,854 vs 81,366 · hash 904 vs 762 EH/s · tx 578,327 vs 485,901 · sats 1,548 vs 1,600 · UTXO -0% vs +0.63%/epoch; console clean; 375px clean) |
| 22 | v1.2 — integrity curve v2 (Joe, 2026-07-18): at-average = 15/20, full 20 only at 1.5× the 3-yr avg, ≤half = 0 — no more free 100 at the saturation cliff | 21 | ☑ DONE (2026-07-18; live 86 SOLID = 17+17+17+15+20, was 100; console clean) |
| 23 | v1.2 cleanup — hardening review + drill sweep over the whole one-day v1.2 wave (17-22): multi-agent review of `5df031b..HEAD`, full drill regression (kill-network, WS-kill, per-origin failures incl. Blockchair-430 fallback), Firefox + 1366×768 gap attempts | 16, 22 | ☑ DONE (2026-07-19; 5-finder review → 4 CONFIRMED fixed + empirically proven [HIGH compUtxo cross-anchor inflation → series-clock anchoring; false-STALE from covered fallbacks; tx in-progress-day clamp; stale-tag pill vs orange titles] + 1 REPORTED-to-Joe [nodes as-of removed by his task-19 spec — restore option in WORKLOG]; drill sweep ALL PASS [WS-kill, 3 per-origin kills silent, page-wide kill: 13/14 tagged + nodes exempt + title STALE + full recovery, reduced-motion, 1280×940 fold 935≤940 live, 1366×768 recorded, 375 clean]; Firefox still absent — gap stays; findings register in WORKLOG.md) |
| 24 | v1.2 cleanup — docs + version truth: README/CLAUDE.md still say "one data source" + v1.0.0; five origins now; fresh screenshot; tag v1.2.0 after 23 | 23 | ☑ DONE (2026-07-19; README: 5-origin data section + fragile-Blockchair caveat + honest "what can't be live" note + brand line + v1.2.0; CLAUDE.md blurb → primary+4 aux; footer → v1.2.0 release link (+4 origins hint); meta description → current panel set; assets/hull.png re-shot at 1280×940 on the orange build via in-page html2canvas-pro (pane screenshots dead today — capture eyeballed faithful, closed-dropdown ghost labels suppressed); `v1.2.0` tagged + GitHub release, notes = condensed 17–23 rows) |
| 25 | v1.2 cleanup — responsive/mid-width polish: 1366×768 + 720-1100px band unaudited since the density passes; mobile type-floor decision; desktop fold bar must survive | 23 | ☐ |
| 26 | v1.2 — branding +50% (Joe, 2026-07-18): wordmark 26px, ship 132px (viewBox-cropped to keep the header short), fold reclaimed via gaps/padding only — chain-tip bottom 935px @1280×940; mobile brand scale rule; integrity math independently audited same session (recompute = displayed 86 exactly) | 21, 22 | ☑ DONE (2026-07-18; console clean, 375px clean) |
| 15 | v1.1 — Clark expansion (Joe, 2026-07-17): layout re-order, minor tier, missing majors, Hull Integrity recomposition | 10, 12 | ☑ DONE (2026-07-17; chain tip → page bottom (survived move live); strip renamed "Bitcoin's Hull Integrity" + visible note; INTEGRITY RECOMPOSED: 5 stats × 20 = current vs own 3-yr avg (nodes / hash / tx-day / sats-per-$ inverted / UTXO 1y-growth vs 3y-avg-growth), linear half-avg→avg curve, all feed-anchored (no wall-clock scoring), sum-of-rounded-rows, live score 80/100 HOLDING with UTXO honestly at 0/20 (set SHRANK 0.5% vs +30%/yr baseline); new majors: Difficulty retarget (1,178 blocks EXACT Clark match, date/est/epoch 476/bar), Chain security (90d 943 vs Clark 942.1, chain work 96.3 bits from full retarget history, rewrite days, trailing-365d revenue $15.3B — Clark semantics, never projected), Blockchain (UTXO hero, chain size 754.6 GB — blockchain.info excludes undo/indexes vs Clark's on-disk 860.3, baked OP_RETURN 0.4 GB, prior-yr block time 10:01 EXACT), Mining reworked (implied 2016-blk hashrate via work-over-window, last adj -5%, realized block times); minor tier: treasuries + Lightning (capacity 4,788.79 BTC / value / nodes / channels / Tor nodes 51.3% — Tor CAPACITY split has NO endpoint, gap named; snapshot date visible, mempool's LN stats run ~a month behind); new origins: blockchain.info charts CORS-verified (cors=true REQUIRED — 0 ACAO without) + 4 mempool.space endpoints; HULL.hist derivation module; 6-agent history research adversarially verified all 4 series in a real browser engine; garbage-BASE drill: DEGRADED + socket-live + aux-immune + recovery, no false staleness on 6h feeds; 375px no-h-scroll; console clean) |

Chunking logic: 01 fixes the look so every later task is pure wiring; 02 is
the single shared data spine; 03–08 are independent panel tasks (any order,
one sitting each); 09 turns a polling page into a live instrument; 11 (added
2026-07-16, Joe) wires the Integrity bar — any time after 02, before 10;
10 ships. The table row order above (…09, 11, 10) is execution order.
Tasks 12–14 are **v1.1** (12–13 added 2026-07-16 by Joe; 14 laid out
2026-07-17 per Joe's "next 3"): two new data sources beyond mempool.space
(Luke Dashjr's CORS-open daily node file; a baked bitcointreasuries.net
snapshot — both verified live in their TASK.md files) plus the socket-only
incoming-flow row. They do not gate v1 and must not start before 10's
closers are done. Priority order = 12 → 13 → 14; when all three tick,
v1.1 is feature-complete (tagging `v1.1.0` is Joe's call).

## "Done" definition (task 10 closes against this)

Page live on GitHub Pages at a public URL; every panel live with real data;
survives: API down (visible stale state, no console errors), tab left open
24 h (no leak, still ticking), phone-width viewport; zero console errors;
README + QA evidence committed; this table all ☑.

## Explicitly OUT of v1 (do not start without Joe)

- Lightning section, version stats, altcoin anything. (Node-COUNT stats:
  promoted out of this list to v1.1 task 12 by Joe, 2026-07-16.)
- Historical charts beyond what a panel needs (no time-series explorer).
- User accounts, settings persistence beyond localStorage, custom nodes.
- Light mode (the Hull is dark; revisit post-v1).
